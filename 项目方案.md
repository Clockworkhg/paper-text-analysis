# CADS Workbench 项目方案

> 语料库辅助话语研究(CADS)工作台 —— 面向新闻媒体话语研究的全流程文本分析工具
>
> 版本:0.3.0 · Python ≥3.10 · 许可:MIT · 仓库:github.com/Clockworkhg/paper-text-analysis

---

## 1. 项目定位

### 1.1 项目是什么

CADS Workbench 是一个**研究级语料库辅助话语分析工具**,服务于论文《媒体话语中的"系统性竞争对手"表征研究》(EU-China 议题新闻语料)。它把大规模新闻文本处理、来源机构规范化、国别识别、KWIC 检索、搭配/修饰语/短语提取、语义韵候选、分组比较和人工复核组织成一条**可复现、可审计、可人工介入**的研究流水线。

### 1.2 方法论框架

项目采用四层理论框架,功能设计与其一一对应:

| 理论层 | 对应功能 |
|---|---|
| 语料库辅助话语研究(CADS) | 语料构建、来源统计、KWIC、搭配统计 |
| 批判话语分析(CDA) | 来源机构合并/国别识别(话语生产位置)、分组比较 |
| KWIC + 搭配 + 语义韵 | 目标词上下文、MI/log-likelihood 搭配指标、语义韵候选 |
| 评价理论 + 框架分析 | 形容词/短语/评价资源候选提取、按机构/国别/立场分组比较 |

核心方法原则(贯穿全部输出):

1. 自动统计用于**发现模式**,不直接等同于研究结论;
2. 所有关键解释必须回到 KWIC 或原文上下文人工复核;
3. 来源合并、国别识别、修饰语提取保留规则、参数与人工修正空间;
4. 输出记录参数、运行环境和工具版本,支持复现;
5. 自动结果在研究写作中应表述为"候选证据/语言模式线索"。

---

## 2. 技术栈

| 类别 | 选型 | 用途 |
|---|---|---|
| 语言 | Python ≥ 3.10(兼容 3.10–3.13) | — |
| 数据处理 | pandas ≥1.5,<3 / numpy | 表格计算、语料统计 |
| Excel 输出 | openpyxl | 全部 xlsx 工作簿(附 README sheet) |
| NLP | spaCy 3.x(en_core_web_sm) | 分词、词性、依存(amod 修饰语抽取) |
| NLP | NLTK | 词性标注增强(缺数据时优雅降级为 UNKNOWN) |
| 字符串/实体 | rapidfuzz | 来源机构模糊合并、国别模糊匹配 |
| 知识库 | requests + tldextract + Wikidata SPARQL/REST | 来源机构国别推断 |
| 可视化 | matplotlib | 国别分布饼图 |
| 文档解析 | python-docx | LexisNexis DOCX 拆分 |
| 桌面 GUI | Tkinter/ttk(标准库,零额外依赖) | 集成工作台 |
| 打包 | PyInstaller ≥ 6 | Windows 独立 exe(cads / cads-gui) |
| 测试 | pytest | 191 个用例 |
| 可选扩展 | RapidOCR + pypdfium2(扫描 PDF 识别)、openai(联网 LLM 过滤) | 按需安装 extras:`[ocr]`、`[online]` |

---

## 3. 系统架构

```
┌────────────────────────────────────────────────────────┐
│ 入口层                                                  │
│  research_tool.py(CLI 主入口)                          │
│  integrated_app.py(Tkinter 桌面应用)                   │
│  pipeline.py(旧版兼容)  build_exe.py(打包)            │
├────────────────────────────────────────────────────────┤
│ 工作流层 shared/                                        │
│  project_workflow.py  项目级 init/import/analyze/…      │
│  cli_pipeline.py      1–5 步流水线调度与 run_config      │
│  pipeline_steps.py    s1–s5 步骤实现 + 分组模式          │
│  corpus_model.py      文档登记表/语料清单/国别回流        │
│  corpus_importers.py  TXT/MD/DOCX/CSV/Excel 导入         │
│  validation.py        复核模板/验证报告/编码者信度        │
│  research_templates.py 7 类语料研究模板                  │
├────────────────────────────────────────────────────────┤
│ 分析引擎层 modules/                                     │
│  txt_modifier_extractor.py  核心引擎(process_txt)       │
│  hebing.py / jiacixing.py 等  遗留步骤(机构合并/词性)   │
│  LexisWordToTxt/            LexisNexis DOCX 拆分器      │
├────────────────────────────────────────────────────────┤
│ GUI 层 modules/gui/                                     │
│  support.py(基类/样式) tool_tabs / pipeline_tabs        │
│  project_workbench  result_browser                      │
└────────────────────────────────────────────────────────┘
```

**关键数据流**(分析主链路):

1. 导入:语料文件写入 `corpus/<规范来源>/*.txt`,头部含 `<TITLE>/<SOURCE>/<DATE>` 元数据;
2. 登记表:`01_corpus/documents.csv` 记录每篇文档的 `document_id`、`source_raw`、`source_normalized`、`country`、命中数等;
3. 合并:s4 将全部 TXT 合并为 `_corpus_merged.txt`,并按登记表注入 `<DOCUMENT_ID>/<SOURCE_NORM>/<CORPUS_ID>/<RUN_ID>` 头;
4. 分析:`process_txt` 切分文档 → spaCy 管线 → 目标词命中 → KWIC/形容词/短语/搭配聚合 → Excel 工作簿;
5. 分组:按所选 `group_by` 模式解析每篇文档的组标签,聚合进 GroupComparison;
6. 复核:`generate_validation_artifacts` 生成人工复核模板、验证报告与方法说明。

---

## 4. 功能清单

### 4.1 流水线五步(1–5)

| 步骤 | 功能 | 输出 |
|---|---|---|
| 1 | LexisNexis DOCX 拆分 → TXT + 统计报告 | `corpus/*.txt`、`report-*.json` |
| 2 | 报告 JSON → 机构统计表 | `source_counts.xlsx` |
| 3 | 机构模糊合并 + 国别识别(规则/启发式/Wikidata 三级,高置信自动采纳) | `merged_sources.xlsx`(WithCountry/CountrySummary/SuggestedOverrides/AutoOverrides)、`03_country/source_countries.csv`、饼图 |
| 4 | 修饰词分析主引擎(见 4.3) | `adjectives_phrases.xlsx` |
| 5 | 词性标注 + 中文翻译增强 | `adjectives_final.xlsx` |

### 4.2 项目级工作流(`research_tool.py project …`)

| 子命令 | 功能 |
|---|---|
| `init` | 创建项目(project.json:语料类型、目标词、模板、历史) |
| `import` | 导入文件夹(自动识别来源子目录)或 CSV/Excel 表(自定义文本/标题/来源/日期/分组列) |
| `analyze` | 重跑登记表 + 分析 + 验证产物,支持 `--mi-threshold`、`--group-by` |
| `groups` | 生成可编辑的自定义分组模板 `01_corpus/group_overrides.xlsx` |
| `review` | 生成复核模板与验证报告,支持双编码员模式(Coder A/B 独立文件) |
| `status` | 项目状态总览(文档数、命中数、已完成产物、下一步建议) |
| `sanity` | Step 0 语料卫生检查(标记污染/空短文档/重复正文/编码异常),报告存档 07_reports |
| `freeze` | 将当前运行固化为不可变快照 `runs/<run_id>/`(参数+语料指纹+模型/规则/算法版本+逐文件哈希) |
| `report` | 生成 Markdown 研究报告(语料概况、来源 Top、分析汇总、验证状态、近期历史) |
| `run` | 导入+分析+复核材料一步完成 |

### 4.3 分析引擎(modules/txt_modifier_extractor.py · `process_txt`)

一次运行产出主工作簿 `adjectives_phrases.xlsx` 的全部 sheet:

- **KWIC**:目标词左右上下文(定性解释入口),含 `Source`/`Source_Normalized`/`Group` 列;
- **Adjectives**:形容词候选(amod 依存 + 系表结构 + 窗口补充),带停用词/代词同义反复质量过滤,近似词频/文档频标准化;
- **Phrases**:短语与词块候选(副词+形容词连续短语等);
- **Collocates / Collocates_Significant**:窗口内内容词共现,含 MI 分数、log-likelihood(G²)、χ² p 值与显著性星号(默认 MI≥3 为显著);
- **SemanticProsodyCandidates**:评价种子词表的积极/消极/混合候选标签(仅供复核);
- **GroupComparison**:分组比较(见 4.4);
- **LLM_Decisions / LLM_Filter_Summary**(可选):联网 LLM 判断修饰语是否成立;
- **Meta / README**:运行参数、分组方式与字段说明。

### 4.4 分组比较(group_by,0.3.0 新增)

分组变量用户可选,贯穿 CLI/GUI,持久化于 project.json、run_config.json 与 Meta 表:

| 模式 | 分组依据 | 数据来源 |
|---|---|---|
| `source`(默认) | 语料 TXT 原始 `<SOURCE>` 表头 | 语料文件本身 |
| `institution` | 规范化媒体机构名 | 文档登记表 `source_normalized` |
| `country` | 来源国别(未匹配回退机构) | 登记表 `country` 列(由 merged_sources.xlsx 三级匹配回流,需人工复核) |
| `custom` | 研究者自定义标签(如立场/倾向类别) | `01_corpus/group_overrides.xlsx`(project groups 生成模板) |

GroupComparison 带 `Group_By` 列标明当前维度;换维度重跑步骤 4 即可。

### 4.5 人工复核与信度

- `06_review/modifier_semantic_review.xlsx`:修饰语/语义韵逐条复核(语义韵、研究举动、立场等编码列);
- `06_review/source_country_review.xlsx`:来源合并与国别复核;
- 双编码模式:Coder A/B 独立复核文件 → 合并后自动计算 Cohen's κ / Krippendorff's α(InterraterReliability sheet);
- `07_reports/`:validation_report.xlsx、method_summary.md、method_limitations.md、research_report.md。

### 4.6 语料研究模板(7 类)

news_lexis(新闻/LexisNexis)、policy、academic、interview、social_media、translation、generic——各自定义推荐元数据字段、编码字段与说明、错误类型、方法焦点和局限,随项目存档至 `00_run_config/research_template.json`。

### 4.7 辅助能力

- **文献知识库**:`lit list/search/index` 子命令,检索 `docs/literature_notes/` 与 `literature_md/`(六大类、40+ 篇方法论文献笔记);
- **OCR**:`ocr` 子命令(RapidOCR)处理扫描版 PDF 文献;
- **国别推断**:手工规则 + 国别/民主国家aliases/城市映射 + Wikidata SPARQL/REST 三级策略,置信度分级;
- **可复现性**:每次运行写入 run_config.json(参数 + Python/包版本 + git commit 快照)。

---

## 5. GUI 详解(integrated_app.py)

单窗口 Tkinter 应用,左侧上下文面板 + 中央 Notebook 工具页 + 顶部状态栏;统一设计令牌(配色)、文件选择行、日志面板、后台线程 + 轮询(界面不卡顿)。全部业务逻辑委托 `shared/` 层,GUI 只做参数输入与结果展示。

| 工具页 | 分组 | 功能 |
|---|---|---|
| 项目工作台 | 研究流程 | 五步向导:初始化 → 导入 → 分析(目标词/MI 阈值/分组方式/词性翻译) → 生成复核(抽样数/双编码) → 生成报告;左栏项目状态灯,右栏输出文件快捷打开 |
| 全流程运行 | 研究流程 | 从 Lexis DOCX 一步到复核材料:步骤勾选、检索目标、国别推断、分组方式 |
| 机构合并+国别 | 语料分析 | 独立运行步骤 2/3(overrides、模糊阈值、联网推断、饼图 TopN) |
| 修饰语分析 | 语料分析 | 独立运行分析引擎(窗口 tokens、短语长度、分段方式) |
| 词性+翻译 | 语料分析 | 为候选词表补 POS 与中文释义 |
| KWIC 分析 | 辅助工具 | KWIC 文本整理为可复核 Excel |
| 自选列合并 | 辅助工具 | 按键列分组求和 |
| JSON 转 Excel | 辅助工具 | 报告 JSON 提取来源统计 |
| 结果浏览与复核 | 结果与复核 | 加载工作簿按 sheet 浏览、筛选、内联复核标注、导出验证报告 |

打包产物 `cads-gui.exe` 即此应用;`cads.exe` 为同名 CLI。

---

## 6. 输出目录布局与数据模型

```
output/ | 项目目录/
├── project.json                  项目状态与历史
├── run_config.json               运行配置(含 00_run_config/ 副本)
├── 00_run_config/                run_config、data_model、research_template
├── 01_corpus/                    documents.csv(登记表)、document_registry.xlsx、
│                                 corpus_manifest.json、group_overrides.xlsx(可选)
├── 02_sources/                   机构统计
├── 03_country/                   source_countries.csv(规范来源→国别表)
├── 04_kwic/  05_modifiers/       步骤产物
├── 06_review/                    人工复核工作簿(双编码文件)
├── 07_reports/                   验证报告、方法摘要/局限、研究报告
├── corpus/                       规范化 TXT 语料(按来源分目录)
├── _corpus_merged.txt            s4 合并语料(注入稳定 ID 头)
├── merged_sources.xlsx           机构合并 + 国别
├── source_counts.xlsx            原始来源统计
├── adjectives_phrases.xlsx       ★ 主分析工作簿
└── adjectives_final.xlsx         词性+翻译增强(可选)
```

数据模型:`corpus ←1:n→ document ←1:n→ analysis_run / review_item`,`document_id` 是 KWIC、搭配、编码与复核结果的统一连接键;模型版本与字段说明存档于 `00_run_config/data_model.json`。

---

## 7. 质量保障

- **测试**:10 个测试文件、**203 个用例全部通过**,覆盖:导入器、语料模型、**语料卫生检查**、抽取核心、手工规则、规范化、流水线(s4 注入/分组映射/回退/**污染拦截**)、项目工作流(group_by 持久化/status 回退/**freeze 不可变性**)、研究模板、分析引擎(分组模式端到端);
- **Step 0 门禁**:任何分析运行前自动检查语料卫生,元数据标记污染直接阻断(可 `--skip-sanity` 逃生,不推荐用于正式研究);
- **输出自律**:每个 Excel 附 README sheet(字段说明 + 参数);运行配置含环境快照 + 语料指纹 + 模型/规则/算法版本;final run 可固化为不可变快照;
- **仓库卫生**:生成物、本地研究数据(`文献/`、`新文献/`、`projects/` 等)默认 gitignore;可复现示例进 `examples/`;
- **方法边界**:文档与输出中反复声明"自动输出为候选证据",国别/语义韵须人工复核。

---

## 8. 开发进度

### 8.1 里程碑时间线

| 阶段 | 内容 |
|---|---|
| 0.1.0 | 初始导入与工程化(基线 README、测试骨架) |
| 0.2.0 研究工作台 | 统一 CLI(run/review/ocr/docs/test)、KWIC/搭配/语义韵/分组比较、复核与验证工作流、方法论文档、可复现性输出 |
| 0.2.1 | 打包、重构准备分发 |
| Phase 1–11 分支系列 | 仓库卫生 → 分析核心与 GUI 分离 → shared GUI 支持 → 工具页/结果浏览器/项目工作台/流水线页拆分 → GUI 包化 → CLI 流水线核心 → 项目工作流边界 → 打包边界 |
| **Phase 12(当前)** | **分组方式可选化**(source/institution/country/custom),国别回流登记表,`project groups` 模板,status 命中数修复;**Step 0 语料卫生检查**(分析硬门禁 + `project sanity` + 导入警告);**研究运行固化**(`project freeze` → 不可变 `runs/<run_id>/` 快照 + manifest:语料指纹/模型版本/规则版本/git commit/逐文件哈希);版本统一 0.3.0 |

### 8.2 当前状态

- 分支 `codex-phase12-grouping-modes`(领先 master,已推送 origin),工作区干净;
- **203 tests passed**;CLI/GUI 全链路端到端冒烟通过;真实项目卫生检查验证(198/198 检出污染);
- 版本三处(VERSION / pyproject / CHANGELOG)已对齐 0.3.0;
- 待办:清理污染语料并重新导入 → 合入 master 打 tag v0.3.0 → 重建 dist exe(RELEASE_CHECKLIST 五项)→ **功能冻结**,进入论文写作期(仅接受 bug 修复)。

---

## 9. 研究应用现状(projects/systemic_competitor)

| 项 | 值 |
|---|---|
| 研究主题 | "系统性竞争对手"话语:EU-China 关系新闻表征 |
| 语料 | 198 篇英文新闻(2019-03 至 2023+,LexisNexis 导出) |
| 来源分布(规范) | AFP 32、FT 30、RFI 23、NYT 21、FP/Deutsche Welle 博客 26、The Times 10、AP 10 等 |
| 目标词 | 19 个:China、Beijing、EU/Europe、systemic rival/competitor、cooperation、confrontation、threat、partnership、dependence、decoupling、assertiveness、BRI/Belt and Road、Ukraine、Taiwan、trade war、protectionism、security |
| 分析产出 | KWIC 11,105 条命中;搭配候选 6,101;形容词候选 3,260;GroupComparison/语义韵候选齐备 |
| 人工复核 | 修饰语语义复核已完成多编码员流程(Coder A / B / B-ChatGPT / C)并调和出 FINAL 版 |
| 报告 | validation_report、method_summary/limitations、research_report 均已生成 |
| 数据卫生 | ⚠ 198/198 篇正文含二次包装标记(已由 Step 0 检查确认),**必须用原始 DOCX/干净正文重新导入后方可重跑分析** |
| 待执行 | ① 清理污染语料、重新导入(Step 0 门禁已就位);② 用 `--group-by institution` 重跑分析;③ 复核国别标签(SuggestedOverrides);④ 编码信度(Krippendorff's α)写入方法章;⑤ KWIC 层语义韵确认记录;⑥ `project freeze --label final` 固化最终运行;⑦ 合并 v0.3.0、停止开发、写论文 |

> ⚠ 数据注意事项:该项目语料导入源为已带 `<SOURCE>:`/`----- BODY -----` 标记的 TXT,二次包装后旧标记留在正文(KWIC 上下文与搭配词表中出现 "BODY"/"body")。重跑分析前建议改用干净正文或 Lexis 原始 DOCX 重新导入,避免标记词污染搭配统计。

---

## 10. 文档与文献体系

**docs/**(15 篇):ARCHITECTURE、DATA_DICTIONARY、METHODOLOGY、WORKFLOW、PROJECT_WORKFLOW、MULTI_CORPUS_IMPORT、RESEARCH_PLAN、RESEARCH_TEMPLATES、VALIDATION、LITERATURE_CLASSIFICATION、RESEARCH_GRADE_PROJECT_DESIGN、MAINTENANCE、PACKAGING、RELEASE_CHECKLIST + literature_notes/。

**literature_md/**(6 大类):01 核心必读(Baker、Partington、Hardt-Mautner、刘明、钱毓芳、Krippendorff、Grimmer & Stewart、Wodak、Reese 框架等)、02 方法工具、03 媒体话语应用、04 评价/情感/框架、05 文本即数据工具、06 低优先级——每篇附 Markdown 摘要笔记,`lit search` 可全文检索。

---

## 11. 打包与分发

- 开发模式:`python research_tool.py …` 或 `pip install -e ".[dev]"` 后使用 `cads` / `cads-gui` 入口;
- 发布模式:`build_exe.py --cli/--gui` 产出 `dist/cads.exe`、`dist/cads-gui.exe`(支持 `--onedir`、`--debug-console`);
- 发布检查单(RELEASE_CHECKLIST):测试 → py_compile → git 状态 → 真实语料冒烟 → 文档四件套(README/examples/QUICKSTART/METHODOLOGY/DATA_DICTIONARY/CHANGELOG/VERSION)→ 研究注意事项声明;
- 可选 extras:`[build]` PyInstaller、`[ocr]` RapidOCR、`[online]` openai。

---

## 12. 后续路线图

### 工程线(短期)

1. **清理污染语料、重新导入**(Step 0 卫生检查已就位,导入时会警告、分析会拦截);
2. 合并 phase1–12 系列入 master,打 tag **v0.3.0**;
3. 按 RELEASE_CHECKLIST 重建并冒烟 dist exe;
4. **功能冻结**:论文写作期仅接受 bug 修复,不重购 NLP 模型、不换依赖、不重构。

### 研究线(主线,关键路径)

1. 重新导入后用 `--group-by institution` 重跑分析(必要时 `country`,先用 SuggestedOverrides 复核标签);
2. 从 Coder A/B/C 复核表计算 Krippendorff's α,写入方法章;
3. SemanticProsodyCandidates 的 KWIC 人工确认记录落实(方法章证据链);
4. 基于机构/国别分组的 GroupComparison 展开核心实证分析与写作;
5. `project freeze --label final` 固化最终运行(manifest 含语料指纹 + 模型/规则/算法版本 + git commit);
6. 归档:tag 仓库 + 冻结快照 + 复核工作簿 + run_config(可复现性包)。

### v1.0 产品化方向(论文完成后,已评审确认)

- **GUI 重构为"研究工作台"**:主界面回答"我的研究做到哪里了"(研究概览 + 流程状态),而非工具箱;核心投资 **Review Workbench**(语义韵逐条键盘编码 UI:1/2/3/4 标签 + Enter 下一条 + 完整语境面板 + 进度),这是工具最有价值、尚未产品化的部分;
- **GUI 框架迁移 PySide6**(不迁 Tauri):QTableView + QAbstractTableModel 承载 1 万行级 KWIC,克隆现有"业务在 shared 层、GUI 只是 adapter"的边界,CLI 永久保留;
- **方法边界嵌入界面**:MI/LL 永远标注"候选排序,非统计推断";语义韵/国别处常驻 ⓘ 说明;国别展示升级为 Evidence 面板(规则/知识库/域名证据 + 置信度 + 接受/修改);
- **Evidence Trail(证据篮)**:研究者在 KWIC/比较行上"加入证据篮",按论文章节导出 Claim→统计模式→KWIC 例证→document_id 的证据链,这是最具差异化的潜在能力;
- **规范化数据存储**:Excel 降级为人机交互格式,内部以 parquet/sqlite 为准(复核结果"导入回流"机制),为换 GUI 解耦;
- **前瞻式 versioned runs 目录**(`runs/run_YYYYMMDD_NNN/` + latest 别名):freeze 快照已提供回溯式等价能力,前瞻式目录待产品化阶段实现;
- 允许 NLP 模型分档(fast=en_core_web_sm / accurate=en_core_web_trf),但**论文期间锁定当前模型不换**,避免结果漂移。

### 技术栈结论(评审确认)

pandas/spaCy/rapidfuzz/openpyxl/Tkinter/PyInstaller 维持不变;不引入 Polars/Tauri/Rust;"不要为了现代而重构"——论文期间底层冻结,工程精力全部让位给研究本身。

---

## 13. 已知限制(研究写作须知)

- 国别推断为三级自动策略,标签属**辅助变量**,必须经 `source_country_review` 人工复核;
- 语义韵候选仅为种子词表提示,最终判定必须回到 KWIC 语境;
- 搭配 MI/LL 为近似指标(非标准语料库工具实现),用于排序候选而非统计证明;
- 词性/依存分析基于 spaCy 英文小模型,对标题体、噪声字符敏感;
- 登记表会在每次 import/analyze 时按当前语料与目标词重建——后续导入若改变 targets,会使登记表命中数与既有分析解耦(status 已做回退显示,但数据上应以分析工作簿为准)。

---

*本文档由项目仓库实际代码与运行状态核验生成,与 0.3.0 版本对应;重大变更后请同步更新。*
